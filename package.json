{
  "name": "csc-307-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "start:client": "cd packages/react-frontend && npm run dev",
    "start:server": "cd packages/express-backend && node backend.js",
    "install:all": "npm run install:client && npm run install:server",
    "install:client": "cd packages/react-frontend && npm install",
    "install:server": "cd packages/express-backend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "concurrently": "^8.2.2",
    "mongoose": "^8.3.2"
  }
}
